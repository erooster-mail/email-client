# install_data(
#   'mail-symbolic.svg',
#   install_dir: iconsdir / 'hicolor' / 'symbolic' / 'actions',
# )

# install_data(
#   'inbox-symbolic.svg',
#   install_dir: iconsdir / 'hicolor' / 'symbolic' / 'actions',
# )